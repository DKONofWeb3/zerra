import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function WalletPage() {
  return (
    <PagePlaceholder
      title="Wallet"
      description="Connected wallets, balances, deposits, and withdrawals."
    />
  );
}
