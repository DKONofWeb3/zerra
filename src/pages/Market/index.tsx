import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function MarketPage() {
  return (
    <PagePlaceholder
      title="Market"
      description="Browse all listed tokens, projects, and bounties. Filter, sort, and discover."
    />
  );
}
